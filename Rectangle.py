# Rectangle.py


def r_area(area_w, area_l):
    r_area_calc = float(area_w * area_l)
    return r_area_calc


def r_perimeter(p_width, p_length):
    r_perimeter_calc = float((p_width + p_length) * 2)
    return r_perimeter_calc
